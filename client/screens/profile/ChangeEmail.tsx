import React from "react";
import { View } from "react-native";

interface EmailProps {
	visible: Boolean;
}

const ChangeEmail = (props: EmailProps) => {
	return <View>ChangeEmail</View>;
};

export default ChangeEmail;
